import type { Metadata } from "next";
import { Halant } from "next/font/google";
import { Inter } from "next/font/google";
import "./globals.css";
import "@/lib/gsap-setup";
import { ServiceWrapper } from "@/components/ServiceWrapper";
import Tag from "@/tag/Tag";
import { getVisualEditScript } from "@/utils/visual-edit-script";
import { Montserrat } from "next/font/google";



export const metadata: Metadata = {
  title: 'Print Hub Targoviste | Professional 3D Printing Services',
  description: 'Expert 3D printing and prototyping services in Targoviste. High-precision industrial prints, rapid turnaround, and custom design solutions for everyone.',
  keywords: ["3d printing, targoviste, prototyping, industrial printing, rapid manufacturing, custom parts"],
  openGraph: {
    "title": "Print Hub Targoviste",
    "description": "Professional 3D printing services for engineers and designers in Targoviste.",
    "type": "website",
    "siteName": "Print Hub Targoviste"
  },
};

const montserrat = Montserrat({
  variable: "--font-montserrat",
  subsets: ["latin"],
});
const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <ServiceWrapper>
        <body className={`${montserrat.variable} ${inter.variable} antialiased`}>
          <Tag />
          {children}
          <script
              dangerouslySetInnerHTML={{
                  __html: `${getVisualEditScript()}`
              }}
          />
        </body>
      </ServiceWrapper>
    </html>
  );
}
